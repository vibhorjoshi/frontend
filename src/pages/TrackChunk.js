import React from "react";

function TrackChunk() {
    return ( <
        section id = "track-chunk" >
        <
        h2 > Track Chunk < /h2> <
        p > Track data insights related to specific product chunks. < /p> <
        /section>
    );
}

export default TrackChunk;