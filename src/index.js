import React from "react";
import ReactDOM from "react-dom";
import App from "./App";
import "./styles.css"; // Optional: if you want to add custom styles

ReactDOM.render( < App / > , document.getElementById("root"));